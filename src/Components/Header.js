import React from 'react';

const Header = () =>{
    return(
        <header>
            <h1>Current <span>Google Alerts</span> for <span>"coronavirus"</span> and <span>"Edmonton"</span></h1>
            <p>Refresh for newest updates</p>
        </header>
    );
}

export default Header;