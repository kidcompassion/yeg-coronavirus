import React from 'react';

const Loader = ()=>{
    return(
        <div className="component--loader">
            <img src="/loading.gif" alt="Loading icon of Salem the cat filing nails"/>
            <h2>Waiting for updates...</h2>
        </div>

    );
}

export default Loader;